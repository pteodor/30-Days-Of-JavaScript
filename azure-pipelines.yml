# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  name: Default

steps:
- script: 'set JAVA_HOME=C:\Program Files\Java\jdk1.8.0_202'

- script: 'npx eslint --ext .js --format checkstyle 30DaysOfJavaScript/ > report.xml'
  displayName: 'Run ESLint and generate report'
  
- script: '
"C:\Program Files\Parasoft\MLP\multilanguage-pack.bat" -tool ESLint -results.file "C:\home\demo_apps\javascript\report.xml" -source.dir "C:\home\demo_apps\javascript\30DaysOfJavaScript"'
